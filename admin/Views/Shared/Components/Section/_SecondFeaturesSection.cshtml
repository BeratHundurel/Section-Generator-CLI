@model SectionViewModel
@inject IUnitOfWork _uow
@{
    Language language;
    if (_uow.Cookie.GetUserLanguageId != 0)
    {
        language = _uow.Language.GetById(_uow.Cookie.GetUserLanguageId);
    }
    else
    {
        language = _uow.Language.GetIsRootLang();
    }
}
<input type="hidden" name="sectionName" value="_SecondFeaturesSection" />
<input type="hidden" name="order" value="@Model.order" />
<input type="hidden" name="langId" value="@language.Id" />
<div class="form-group">
    <label>Başlık</label>
    <input type="text" class="form-control" name="title" value="@Model.title" />
</div>
<div class="form-group">
    <label>Feature Left Title</label>
    <input type="text" class="form-control" name="paramText4" value="@Model.paramText4" />
</div>

<div class="form-group">
    <label>Feature Text</label>
    <input type="text" class="form-control" name="paramText1" value="@Model.paramText1" />
</div>

@await Component.InvokeAsync("FileUploadModal",
                    new AdminFileUploadViewModel
                    {
                        FileUploadEntity = new FileUploadEntity
                        {
                            FileAccept = "image/jpg, image/png, image/jpeg",
                            Label = "Fotoğraf",
                            FileId = 1,
                            FileType = "image",
                            MediaId = Model.paramMediaId1.ToString()
                        },
                        AdminFileUploadModal = new AdminFileUploadModal
                        {
                            ModalTitle = "Right Photo",
                        }
                    })
<br />
@await Component.InvokeAsync("FileUploadModal",
                        new AdminFileUploadViewModel
                    {
                        FileUploadEntity = new FileUploadEntity
                        {
                            FileAccept = "image/jpg, image/png, image/jpeg",
                            Label = "Fotoğraf",
                            FileId = 2,
                            FileType = "image",
                            MediaId = Model.paramMediaId2.ToString()
                        },
                        AdminFileUploadModal = new AdminFileUploadModal
                        {
                            ModalTitle = "Left Photo 1",
                        }
                    })
<br />
@await Component.InvokeAsync("FileUploadModal",
                        new AdminFileUploadViewModel
                    {
                        FileUploadEntity = new FileUploadEntity
                        {
                            FileAccept = "image/jpg, image/png, image/jpeg",
                            Label = "Fotoğraf",
                            FileId = 3,
                            FileType = "image",
                            MediaId = Model.paramMediaId3.ToString()
                        },
                        AdminFileUploadModal = new AdminFileUploadModal
                        {
                            ModalTitle = "Left Photo 2",
                        }
                    })
<br />
@await Component.InvokeAsync("FileUploadModal",
                        new AdminFileUploadViewModel
                    {
                        FileUploadEntity = new FileUploadEntity
                        {
                            FileAccept = "image/jpg, image/png, image/jpeg",
                            Label = "Fotoğraf",
                            FileId = 4,
                            FileType = "image",
                            MediaId = Model.paramMediaId4.ToString()
                        },
                        AdminFileUploadModal = new AdminFileUploadModal
                        {
                            ModalTitle = "Left Photo 3",
                        }
                    })